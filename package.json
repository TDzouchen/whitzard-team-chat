{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "rename": "mv dist docs",
    "build": "tsc && vite build && cp config.html ./dist && cp inside-iframe-target.html ./dist && cp iframe.html ./dist && cp iframe-message.html ./dist && cp inside-iframe-target-message.html ./dist && cp -r auto-connection-twilio-trigger-button ./dist && npm run rename",
    "preview": "vite preview"
  },
  "dependencies": {
    "monaco-editor": "^0.33.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "@vitejs/plugin-react": "^1.3.0",
    "typescript": "^4.6.3",
    "vite": "^2.9.7",
    "vite-plugin-monaco-editor": "^1.1.0"
  }
}
